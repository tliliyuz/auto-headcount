{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://auto-headcount.internal/contracts/browser-job-collect.task.v1.schema.json",
  "title": "browser_job_collect task payload v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sourceConnectionId",
    "userId",
    "deviceId",
    "contractId",
    "externalId"
  ],
  "properties": {
    "sourceConnectionId": {
      "type": "string",
      "format": "uuid"
    },
    "userId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "pattern": "^[A-Za-z0-9._:@/-]+$"
    },
    "deviceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "pattern": "^[A-Za-z0-9._:@/-]+$"
    },
    "contractId": {
      "const": "liebide-job-detail-v1"
    },
    "externalId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "pattern": "^[A-Za-z0-9._:@/-]+$"
    },
    "expectedTitle": {
      "description": "发现阶段列表卡片上的职位标题，作为详情合同的内容校验期望值，仅用于批量发现后创建的详情任务。",
      "type": "string",
      "minLength": 1,
      "maxLength": 500
    }
  }
}
