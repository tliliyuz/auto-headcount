# 零推荐职位激活系统 Agent 指南

本文件定义项目的开发入口和轻量规范驱动开发流程。产品行为、架构、数据模型和外部接口分别以 `docs/` 中对应文档为准。

## 开发原则

- MVP 是内部运营后台，不建设企业客户自助门户、计费或复杂多租户。
- 任何业务行为先写入产品规范和验收标准，再编写生产代码。
- MCP、LLM、短信、邮件和浏览器采集必须经适配器接入，业务模块不得依赖供应商原始字段。
- 生产实现前先用脱敏 Fixture 写出会因目标行为缺失而失败的测试。
- 只实现当前里程碑，机器人外呼、向量检索和自动推荐不得提前进入 MVP。
- 真实密钥、真实联系方式、完整简历和可使用的落地页令牌不得进入 Git、日志或测试夹具。

## 开发门禁

```text
确认唯一需求口径
  → ADR 检查
  → 写验收场景与接口契约
  → RED：确认测试因目标行为缺失而失败
  → GREEN：最小实现
  → REFACTOR：持续绿灯下整理
  → 受影响模块完整验证
  → 同步规范、变更记录和验证结果
  → 代码审查
```

纯文档、注释和不可执行配置可以不制造业务 RED，但必须执行适用的链接、格式、Schema 或配置检查。

## 权威文档矩阵

| 主题 | 唯一权威来源 |
|---|---|
| 项目目标、用户和范围 | `docs/00-project-charter.md` |
| 产品流程、规则和交互 | `docs/01-mvp-requirements.md` |
| 模块边界和部署原则 | `docs/02-architecture.md` |
| 实体、状态与数据保留 | `docs/03-data-model.md` |
| MCP 能力与接入规则 | `docs/04-mcp-integration.md` |
| 阶段与交付顺序 | `docs/05-roadmap.md` |
| 数据安全与触达门禁 | `docs/06-security-compliance.md` |
| 可执行验收条件 | `docs/07-acceptance-criteria.md` |
| 开发、测试和交付流程 | `docs/08-development-workflow.md` |
| 页面、交互与接线状态 | `apps/web/docs/FRONTEND.md` |
| 长期技术取舍 | `docs/decisions/ADR-NNN-*.md` |
| 已交付变更与验证状态 | `CHANGELOG.md` |

同一事实只在一份权威文档中完整定义，其他文档使用链接或简短投影。发现冲突时，先修正文档，再修改测试与实现。

## ADR 检查

出现以下任一情况时，应先建立 ADR：

- 选择会长期绑定项目的核心框架、数据库、部署平台或任务队列。
- 改变服务边界、数据所有权或信任边界。
- 引入难以兼容的公共 API、持久化格式或外部承诺。
- 改变敏感信息处理、授权、保留、删除或审计策略。
- 方案上线后迁移成本高或难以安全回滚。

局部、可替换、低成本回滚的实现细节不需要 ADR。

## 当前已确认产品基线

- 产品形态：内部运营后台，加候选人使用的脱敏职位落地页。
- 沉睡职位：发布已满 7 天且不超过 30 天、仍有效、有效推荐数为 0。
- 落地页：隐藏公司名称；薪资只展示范围；工作地点只精确到城市。
- 触达：MVP 必须经人工审核，不允许无审核自动群发。

## 完成声明

交付时必须区分：

- `specified`：规范和验收条件已确认；
- `implemented`：代码已实现但尚未完成全部验证；
- `verified`：已实际运行规定命令并通过。

不得把文档完成或代码存在描述为功能已经验证。
